#include "integration_tests/support/AccumulatorFixture.hpp"

// The fixture is header-only for now; this translation unit exists so the support
// library has a compiled source and is the place to grow non-trivial setup logic.
